git config --global --replace-all user.name lrakshathabhat1
git config --global --replace-all user.email lrakshathabhat2@gmail.com

git init

git add file1
git commit -m "file saved"

git add file1
git commit -m "file saved"

git show 32bf

git log -n 1 --pretty=format:"%h - %an, %ar : %s" 32bf

